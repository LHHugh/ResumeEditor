# 更新日志 · Changelog

## v1.4.3 — 2026-09
- **自定义模块新增「个人评价格式」**：新增自定义模块时可选五种内部结构 —— 更多信息 / 教育背景 / 项目经验 / 个人评价（一段多行评价文字）/ 个人技能。
- 个人评价格为对象结构（`{ aboutme_desc }`），与内置「个人评价」模块同版式渲染；编辑器为单个多行文本域，无条目增删。
- 预览支持行内直接编辑评价内容，多行自动换行；内容为空时正文不渲染（与内置行为一致）。
- 格式持久化与旧数据形状推断均已兼容对象结构。

## v1.4.2 — 2026-09
- **自定义模块新增「教育背景格式」「项目经验格式」**：主区域与侧栏新增自定义模块时可选四种内部结构 —— 更多信息 / 教育背景（时间起止 + 学校 + 专业 + 学历）/ 项目经验（名称 + 角色 + 时间 + 简介 + 内容）/ 个人技能（分组标签）。
- **格式随数据持久化**（`_formats` 记录）：条目删空后格式不丢失，再次添加仍按原格式生成；旧数据按首条结构自动推断并回填。
- 编辑器按格式渲染对应字段（含时间起止 range 输入、多行文本域）；预览按格式渲染，与内置「教育背景 / 项目经验」模块同版式，且支持行内编辑。
- 导出的 JSON 不含内部格式记录，保持数据干净。

## v1.4.1 — 2026-09
- **修正预览页 A4 尺寸**：侧边栏模板预览宽度由 900px 统一为标准 A4（794×1123px @96dpi），侧栏占比与打印一致（1/3 页宽），屏幕预览与导出 PDF 版面完全对应。
- **模块管理支持长按拖动排序**：「主区域模块」与「侧边栏模块」均可按住模块标签约 0.3 秒后拖动，实时预览插入位置，松手即保存并同步到简历预览。
- 基于 Pointer 事件实现，鼠标与触屏通用；拖动中自动抑制页面滚动与文字选中。
- 仅允许同组内排序（主区域 / 侧边栏互不混淆）；点击删除按钮不会误触拖动。

## v1.4.0 — 2026-09
- **个人技能改为分组式结构**：技能按「分组」组织（如编程语言 / 前端框架），每个分组可命名，组内每条技能可独立编辑、删除、新增。
- 预览中支持直接编辑技能分组名与技能项（所见即所得）。
- **侧边栏模块支持两种格式**：新增自定义侧栏模块时，可选「个人技能格式」（分组标签）或「更多信息格式」（内容 + 时间）。
- 主区域自定义模块同样支持上述两种格式选择。
- 兼容旧版扁平字符串技能数组，加载时自动迁移为分组结构。
- 同步更新 README（版本日志、赞助二维码说明）。

## v1.3.0 — 2026-09
- 基本信息联系方式（手机 / 邮箱 / 自定义标签等）支持自由增删改。
- 自定义模块可加入侧边栏区域。

## v1.2.0 — 2026-09
- 模块管理：大模块（教育 / 工作 / 项目 / 技能 / 更多信息 / 作品 / 评价）自由增减。
- 自定义模块：输入名称即可新增，内部结构沿用「更多信息」格式（内容 + 时间）。
- 侧边栏模板支持单独增删「侧边栏模块」。

## v1.1.0 — 2026-09
- 账号系统：注册 / 登录（加盐 SHA-256），数据按账号命名空间隔离，互不覆盖，支持退出切换。
- 非强制赞助弹窗（打印后弹一次 / 随时可点开），关闭不影响功能。

## v1.0.0 — 2026-09
- 三套模板（默认 / 简易 / 侧边栏），主题色与标签色自定义。
- 左侧表单编辑 + 右侧 A4 实时预览；「直接编辑预览」所见即所得模式。
- 头像本地上传（圆角长方形、自动缩放、可粘贴 URL）。
- 导出 / 导入 `resume.json`（兼容 visiky 数据结构）。
- 一键打印 / PDF：A4 满幅、无默认白边。
- 中英界面切换。
